import React from "react";
import ReactDOM from "react-dom/client";
import AudiomarkAI from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <AudiomarkAI />
  </React.StrictMode>
);
